{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dealseeker.local/schemas/add-offer-draft.schema.json",
  "title": "AddOfferDraft",
  "description": "Draft and API request contract for add-offer flows. Mirrors DealsSeeker.Shared.Contracts.AddOffer.AddOfferRequest. Spec IDs: ADD.OFFER.LAYOUT.001, ADD.OFFER.IMAGE.001, ADD.OFFER.LOCATION.001, ADD.OFFER.DESCRIPTION.TAGS.001, ADD.OFFER.TAGS.SUGGESTIONS.001, OFFERS.PHOTOS.CAROUSEL.001, OFFERS.IMAGE.RENDERING.001",
  "type": "object",
  "required": [
    "description",
    "tags",
    "images"
  ],
  "properties": {
    "description": {
      "type": "string"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Tags are normalized and de-duplicated server-side."
    },
    "images": {
      "type": "array",
      "description": "Ordered photo list for offer preview and detail carousel.",
      "items": {
        "$ref": "offer-image.schema.json"
      },
      "minItems": 0
    },
    "location": {
      "oneOf": [
        {
          "$ref": "offer-location.schema.json"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "additionalProperties": false
}
